# ASTRA State Engine

Owned by Person 2.

This service combines perception output with relevant telemetry inputs to produce `AstronautStateOutput`.

It does not handle database persistence, alert delivery, synchronization, or frontend APIs.